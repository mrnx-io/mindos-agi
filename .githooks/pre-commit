#!/bin/sh
set -e

python3 packages/brand-system/bin/compile-system.py

if ! git diff --quiet -- packages/brand-system; then
  echo "Brand system outputs out of sync. Commit regenerated files." >&2
  exit 1
fi
